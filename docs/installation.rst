.. highlight:: shell

============
Installation
============


Stable release
--------------

To install loaded-diceware, run this command in your terminal:

.. code-block:: console

    $ pip install loaded_diceware

This is the preferred method to install loaded-diceware, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for loaded-diceware can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/rlindsgaard/loaded_diceware

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/rlindsgaard/loaded_diceware/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/rlindsgaard/loaded_diceware
.. _tarball: https://github.com/rlindsgaard/loaded_diceware/tarball/master
